{% extends 'cs201/drill.twig' %}

{% block subject %}Drill 2 - User Input{% endblock %}

{% block summary %}
{{parent()}}
<p>Using the Latitude/Longitude converter from the last drill with NetBeans, enhance the program to support retrieving the value from the user at runtime. Your output should look like the following:</p>
<blockquote>
	<code>
		Please enter a coordinate: [User enters a double]<br/>
		Your coordinate is [degrees] degrees, [minutes] minutes and [seconds] seconds.
	</code>
</blockquote>

<p>You may reuse your code from the previous drill or <a href='/uploads/cs201/LatLong.java'>download the solution</a> and start there. Adding the .java file to a NetBeans project may be unintuitive; I will demonstrate the process at the beginning of class.</p>

<h2>Requirements</h2>
<p>The final program that you develop <em>must</em> contain:</p>
<ul>
	<li>A NetBeans project containing your code</li>
	<li>A <code>System.out.print()</code> to prompt the user</li>
	<li>The use of a Scanner object to retrieve the double that the user types in to the console. The Scanner should fail if the user enters anything other than a double.</li>
	<li>A <code>System.out.printf()</code> with a format string to output the coordinates</li>
</ul>
{% endblock %}

{% block notes %}
{% endblock %}

{% block answer %}
{{parent()}}
<p><a href='/uploads/cs201/ScannerLatLong.java'>ScannerLatLong.java</a></p>

<pre class='brush: java; toolbar: false;'>
import java.util.Scanner;

class ScannerLatLong {

	public static void main(String[] args) {
		Scanner keyboard = new Scanner(System.in);

		//Take a given latitude or longitude in decimal format
		System.out.print("Please enter a coordinate: ");
		double input = keyboard.nextDouble();

		//Grab the degrees component by casting to an integer (truncation)
		int degrees = (int)input;

		//Use a temporary variable to grab the decimal component of the coordinate
		//Multiply by 60 to get the minutes
		double tmp = (input % 1) * 60;

		//Cast the minutes to an integer, truncating the decimal component
		//The decimal component will end up being the seconds
		int minutes = (int)tmp;

		//Use the temporary variable again to find the seconds using the same method as the minutes
		double seconds = (tmp % 1) * 60;

		//Print the results to the screen
		System.out.printf("Degrees: %d, Minutes: %d, Seconds: %6.2f\n", degrees, minutes, seconds);
	}
}
</pre>
{% endblock %}
